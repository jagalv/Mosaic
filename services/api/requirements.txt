# Mosaic API — pinned for reproducible local dev.
# Bump deliberately; don't float these in a data/AI app where behavior matters.
fastapi==0.115.6
uvicorn[standard]==0.34.0
sqlalchemy==2.0.36
alembic==1.14.0
# psycopg 3 (binary build = no local libpq/compiler needed on Windows).
psycopg[binary]==3.2.3
pydantic-settings==2.7.0
# SEC EDGAR ingestion HTTP client (timeouts, headers).
httpx==0.28.1
# HTML -> text for filing segmentation (uses stdlib html.parser; no compiled dep).
beautifulsoup4==4.12.3
# pgvector SQLAlchemy types for the Milestone 3 embedding column (vector(384)).
pgvector==0.3.6
# Local embeddings (BAAI/bge-small-en-v1.5) for Milestone 3 RAG. Pulls torch;
# install CPU-only torch FIRST to avoid the ~2.5GB CUDA wheel:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt
sentence-transformers==3.3.1
# Gemini client for grounded answers (Milestone 3). Needs GEMINI_API_KEY.
google-genai==1.16.0
# Auth (Milestone 4a): argon2 password hashing + JWT session cookies.
argon2-cffi==23.1.0
PyJWT==2.10.1
# Test runner for the golden fixture test.
pytest==8.3.4

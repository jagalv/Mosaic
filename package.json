{
  "name": "mosaic",
  "version": "0.0.0",
  "private": true,
  "description": "AI-powered investment research operating system (monorepo root).",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace apps/web"
  },
  "engines": {
    "node": ">=20"
  }
}
